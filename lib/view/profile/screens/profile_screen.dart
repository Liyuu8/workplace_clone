import 'package:flutter/material.dart';

class ProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO:
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('TODO: ProfileScreen'),
      ),
    );
  }
}
