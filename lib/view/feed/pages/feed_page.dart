import 'package:flutter/material.dart';

class FeedPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO:
    return Scaffold(
      body: Center(
        child: Text('TODO: FeedPage'),
      ),
    );
  }
}
