enum SignUpOrLogInMode { SIGN_UP, LOG_IN }

enum UserDetailEntryFieldLabel { FULL_NAME, PASSWORD, ORGANIZATION_NAME }

enum BottomSheetContentType { ORGANIZATION_SIZE, JOB_TITLE }

enum GroupSortAndFilter {
  RecentlyVisited,
  Name,
  Favorites,
  Top,
  LatestActivity
}

enum PostFrom { FEED, GROUP, PROFILE }
